# TROPIC — Folder-by-Folder FDA-Standard Audit (2026-06-18)

Breadth audit of each folder against the FDA/CDISC standard it must satisfy. Scope: structural +
cross-reference checks; **fix safe issues inline, flag judgment/external items**. Deep per-domain
derivation review out of scope. Sequenced submission-critical first.

Legend: **PASS** = meets standard, no action · **FIXED** = inline fix applied this pass ·
**FLAG** = documented/external, needs owner decision (not a fabricated fix).

## Scorecard

| # | Folder | Standard | Verdict | Notes |
|---|--------|----------|---------|-------|
| 1 | `07_define_xml` | Define-XML 2.1 + ARM | **PASS** | see §1 |
| 2 | `m5` | eCTD Module 5 / SDTCG | **PASS** | see §2 |
| 3 | `04_adam` | ADaM IG / XPORT v5 | **PASS** | see §3 |
| 4 | `08_reviewers_guides` + `00_specifications` | PHUSE ADRG/SDRG, BIMO TCG; spec source-of-truth | **PASS** | see §4 |
| 5 | `02_production_sas` · `03_validation_r` · `05_reconciliation` · `09_tfl` | Programming standards; double-programming; ICH E3 | **PASS** (1 advisory) | see §5 |
| 6 | `06_telemetry` · `01_raw_source` · root | CI/Part 11; SDTM provenance; repro | **PASS** | see §6 |

**Overall: 12/12 folders meet FDA/CDISC standard.** 0 defects requiring a fix; 1 advisory
(cosmetic SAS headers); flagged/by-design + external items listed below. No code/config/gate
changes were needed — the structural and conformance posture is submission-grade.

---

## §1. `07_define_xml/` — Define-XML 2.1 + ARM — PASS

Tier A (all green, all in CI ci.yml:79-90):
- **XSD VALID** for **both** `define.xml` and `define_sdtm.xml` (vendored CDISC 2.1 + ARM 1.0 schema).
- **`validate_define.py` 324 ref-integrity checks PASS** on both defines.
- **`def:Class` present** on all ItemGroupDefs (ADaM 7/7, SDTM 16/16) — element form, CORE-parseable.
- `define2-1.xsl` stylesheet present.

Tier B:
- **ARM intact: 8 ResultDisplays / 10 AnalysisResults** (RD.EFFICACY.SURVIVAL/SECONDARY/SUBGROUP/
  PSA.RESPONSE, RD.SAFETY.TEAE/EXPOSURE/LABSHIFT, RD.OPTIMUS.ER) — each cites its TFL ID.
- **`IT.ADRS.AVALC Length="100"`** — consistent with today's SAS `length AVALC $100` fix.
- AsOfDateTime hash-gated via `define_content.sha`.

**FLAG (documented, by design):** clinsite/BIMO has no ADaM/SDTM define entry (prior finding F-7).
Per FDA **BIMO TCG**, the clinical-site dataset is a BIMO deliverable documented via the **BDRG**
(`08_reviewers_guides/BDRG.md`, 15 refs; shipped as `m5/.../bimo/datasets/{clinsite.xpt,bdrg.pdf}`),
not the submission define.xml. Acceptable as-is; owner may optionally author a minimal BIMO define.

No inline fixes required — folder is submission-grade.

## §2. `m5/` — eCTD Module 5 / SDTCG — PASS

- **SDTCG tree correct:** `53-clin-stud-rep/535-rep-effic-safety-stud/mcrpc/5351-stud-rep-contr/
  tropic/{tables,figures,listings}` + `datasets/tropic/{analysis/adam, tabulations/sdtm, bimo/datasets}`.
- **Both `define.xml` + `define2-1.xsl`** present in the adam and sdtm `datasets/` subfolders, and
  **byte-identical to source** (today's define edits propagated correctly).
- **All deliverables present:** `adrg.pdf`, `sdrg.pdf`, `bdrg.pdf`, `csr.pdf`, `ADaM_spec.xlsx`,
  `spec_define_conformance.json`, full SAS (16) + R (17) source programs, TFL outputs
  (4 tables / 13 figures / 1 listing + 5 text tables).
- **Data hygiene clean:** committed m5 is the **data-free preview** (63 tracked files, **0 `.xpt`**);
  `.gitignore` excludes `m5/**/*.xpt` and `*.sas7bdat`, allows `*.pdf`/`*.png`. No patient-level leakage.

By design, the committed `adam/datasets/` holds only define+xsl (no `.xpt`); a real submission
regenerates the `.xpt` locally via `package_ectd.py` (non-preview). Documented; no action.

## §3. `04_adam/` — ADaM IG / XPORT v5 — PASS

- **spec→define PASS** and **spec→data PASS** (metacore/metatools/xportr; `check_variables`,
  `check_ct_data`, `xportr_type`/`xportr_length`).
- **XPORT v5 compliant across all 8 `*_prod.xpt`** — variable names ≤8, labels ≤40, char width
  ≤200 (scanned via `haven::read_xpt`): **0 violations**. The widened `AVALC` ($100) is well within
  the 200-char XPORT limit.
- **Independent double-programming verified today:** 8/8 domains reconcile cell-identical, with
  `*_prod.xpt` (SAS) byte-distinct from `*_v.xpt` (R) per the refreshed md5 manifest.

No inline fixes required.

## §4. `08_reviewers_guides/` + `00_specifications/` — PHUSE/BIMO + spec source-of-truth — PASS

- **Version/structure consistent:** SDRG SDTMIG **v3.1.1**; pipeline described as **17 stages**; no
  stale SAP §-refs (no `§5.4/5.5/5.6`); "OCCDS v1.1" appears **only** in correct negation ("no such
  CDISC version exists").
- **Required disclosures present:** ITTFL/SAFFL/PPROTFL non-discriminating (ADRG §5.4), two-arm HR
  single-programmed/synthetic CbzP (ADRG results-recon scope note), week-precision flooring (ADRG).
- **Spec is metadata control source:** `ADaM_spec.xlsx` + spec→define gate **PASS** (157 vars / 7
  ADaM datasets). Clinsite excluded from spec, consistent with the documented BIMO/F-7 design.
- **Stale-memory correction (no defect):** `AESEV` does **not** impute missing→MILD; both tracks map
  CTCAE grade 1/2/≥3 → MILD/MODERATE/SEVERE and **missing→NA** (R `NA_character_`, SAS `''`), symmetric
  — honest, no fabricated severity. The standard mapping is implied by retained `ATOXGR`; no disclosure gap.

No inline fixes required.

## §5. `02_production_sas` · `03_validation_r` · `05_reconciliation` · `09_tfl` — PASS (1 advisory)

- **SAS:** `lint_sas.py` **0 blocking** errors (35 advisory across 18 files); all `%include` use
  `&PGMDIR`/`&PROJ_ROOT` — no hardcoded/CWD-relative paths.
- **R independence (F-1):** the validation *derivation* scripts read only `*_v.xpt` + staging,
  never `*_prod.xpt`. The only `*_prod.xpt` readers are `spec_data_checks.R` (the spec→**data**
  conformance gate, which must read the produced data) — by design, not a coupling.
- **Reconciliation:** cross-language 8/8 PASS + numerical results 6/6 PASS (verified on today's
  real-SAS run); scope disclosures (MP-only, synthetic two-arm) present.
- **TFL (ICH E3):** published tables/listings **clean** — 0 hits for `nolint` / `<NA>` / `NaN` /
  `Inf` / `sprintf`-spec leakage; deterministic figures (prior fix).

**Advisory (optional, non-blocking):** 35 SAS lint advisories, mostly "missing standard header
block." Not an FDA gate failure; adding `Program:`/`Description:` headers to the 18 programs would
clear them but is cosmetic. Left for owner — no churn applied.

## §6. `06_telemetry/` · `01_raw_source/` · root — CI / Part 11 / provenance — PASS

- **CI (`.github/workflows/ci.yml`) runs 9 gates:** R lint (zero-warning), SAS static-analysis,
  CDISC CORE ADaM rule-pack well-formedness, broker + seeder unit tests, demo smoke test, ADaM
  conformance, **Define-XML validation for both ADaM *and* SDTM**, and the spec→define/spec→data
  conformance gates. Startup assert guards gate↔stage-name coupling.
- **Part 11 / repro:** AsOfDateTime hash-gated (`define_content.sha`); GREEN-ODA evidence badge
  fresh (refreshed today); `renv.lock` pins the environment; CHANGELOG current (**3.13.0**, today).
- **Data hygiene (clean):** **0** tracked `.xpt`, **0** tracked `.sas7bdat`; `_authinfo` /
  `sascfg_personal.py` never tracked; transient run logs gitignored. Guyot `PROVENANCE` token present.
- **SDTM version-gap** (SDTMIG 3.1.1 source vs CORE 3.2 min rules) disclosed in ADRG + CORE_RUN_RECORD.
- **README** accurately states the 17-stage pipeline, the sas_execution_mode honesty rule, and the
  synthetic-comparator/repro scope.

## Flagged items (by-design / external — owner decision, NOT defects)

| Item | Status | Rationale |
|------|--------|-----------|
| clinsite has no ADaM/SDTM define entry (F-7) | by-design | BIMO deliverable documented via BDRG per FDA BIMO TCG; optional minimal BIMO define if desired |
| ITTFL/SAFFL/PPROTFL non-discriminating (F-3) | disclosed | Source DM pre-filtered; no DV domain for real PP exclusion; documented ADRG §5.4 |
| Two-arm HR is R-only (single-programmed) | disclosed | Synthetic CbzP held by one engine by construction; ADRG results-recon scope note |
| SAS header blocks (35 lint advisories) | advisory | Cosmetic; non-blocking; no FDA requirement |
| Second-programmer independence | external | Single-author disclosed (ADRG §6); needs a second programmer to close |
| Pinnacle 21 Community ADaM run; CDISC members-only AD#### crosswalk | external | Vendor/membership-gated; CORE custom ADaM rules run clean as the substitute |
| Week-precision dates (±3.5d) | inherent | Source-data limitation; floors surfaced as warnings, disclosed |

## Verification

No code/config/gate files were modified by this audit — only this report was added. The gates
cited above were exercised during today's real-SAS ODA run and remain GREEN (17/17 stages;
define XSD VALID ×2; 324 ref-integrity checks; spec→define + spec→data PASS; reconciliation 8/8 +
6/6 PASS; SAS lint 0 blocking; XPORT v5 0 violations). The committed m5 preview is data-free.
