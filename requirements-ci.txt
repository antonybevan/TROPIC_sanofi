# Top-level Python dependencies for the data-free CI gates (Python 3.10, Ubuntu).
# Pinned to the versions verified during the 2026-08-05 pipeline audit and the
# local 2026-08-05 verification run. Transitive dependencies still resolve at
# install time; a full locked environment (e.g. uv lockfile) is future work.
pyreadstat==1.2.9
pandas==2.3.3
lxml==6.1.1
saspy==5.107.1
pyyaml==6.0.3
openpyxl==3.1.5
usdm==0.66.0
jsonschema==4.25.1
pytest==8.4.2
