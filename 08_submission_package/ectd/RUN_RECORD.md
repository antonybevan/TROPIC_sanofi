# eCTD Backbone + Study Tagging File — Run Record (refreshed 2026-08-14)

Generated by `platform/build_ectd_backbone.py` / `materialize_ectd.py`. Closes remediation
item **#5** (no eCTD backbone / STF) from `06_qc_evidence/audit/run_records/SUBMISSION_STANDARDS_REMEDIATION.md`.
Leaf counts below were re-verified against the current on-disk sequence on **2026-08-14**.

> **Two eCTD paths (no overlap):** `package_ectd.py` (pipeline stage 36) builds the
> Module-5 **source package tree** (`08_submission_package/m5/`: XPT datasets + a co-located `define.xml` per folder).
> `build_ectd_backbone.py` + `materialize_ectd.py` (pipeline stages 37-38) then **wrap that same package
> source** into the DTD-valid eCTD **sequence** under `08_submission_package/ectd/0000/` (backbone `index.xml`,
> STF, US-regional metadata, checksums). The first produces the content; the second
> assembles the submission sequence around it.

## What was generated (sequence 0000)

```
08_submission_package/ectd/0000/index.xml                    eCTD backbone (root ectd:ectd, dtd 3.2)
08_submission_package/ectd/0000/index-md5.txt                MD5 of index.xml
08_submission_package/ectd/0000/m1/us/us-regional.xml        FDA regional backbone (placeholder app metadata)
08_submission_package/ectd/0000/m5/.../tropic/stf-tropic.xml Study Tagging File (root ectd:study, us-stf 2.3)
08_submission_package/ectd/0000/util/dtd/*.dtd               checksum-pinned official DTDs
08_submission_package/ectd/0000/util/style/*.xsl             checksum-pinned official stylesheets
```

- Machine-checkable current inventory: source_package_files=97; indexed_m5_leaves=98; checksum_leaves=99.
- Current sequence integrity (complete-surface validation of `08_submission_package/ectd/0000/` on 2026-08-14):
  **97** source package files under `08_submission_package/m5/`; **98** indexed sequence `m5/...`
  `xlink:href` targets after adding the STF leaf; **0** missing hrefs; **0** unexpected/unindexed
  sequence files. Backbone `index.xml` carries **99** checksum-bearing leaves (98 m5 leaves + 1
  regional leaf).
- Historical note: an earlier data-free preview was recorded as 84/84 leaves (2026-06); do not
  use that figure against the current materialized sequence.

## Standards basis (researched)

- **ICH eCTD STF Specification v2.6.1** — STF root `ectd:study`; `study-identifier`
  (title / study-id / `category name="type-of-control"`) + `study-document` /
  `doc-content` / `file-tag`; leaf model with `checksum-type="MD5"` / `operation`.
  TROPIC tagged `active-control-without-placebo` (cabazitaxel vs mitoxantrone).
- **ICH eCTD Specification v3.2.2** — backbone `index.xml`, `index-md5.txt`, module-5
  heading element `m5-3-5-1-study-reports-of-controlled-clinical-studies-pertinent-to-
  the-claimed-indication`, and the 53-clin-stud-rep / 535-rep-effic-safety-stud /
  5351-stud-rep-contr folder layout (matches the existing `08_submission_package/m5/` tree).
- **FDA us-stf v2.3** file-tags for datasets, defines, programs, reviewer guides, and
  study-report-body leaves. The generator now fails closed if a dataset XPT, Define-XML,
  program, or controlled-study-report body/appendix file would be emitted untagged.

## QC (all PASS — re-verified 2026-08-14)

- Well-formed XML — `index.xml`, `us-regional.xml`, `stf-tropic.xml` parse cleanly
  under both Python ElementTree and `xmllint`.
- Module-5 sequence parity — **98** materialized sequence m5 files ↔ **98** indexed m5 hrefs;
  **0** missing; **0** unindexed.
- Checksums — checksum-bearing leaves in `index.xml` resolve on disk under `08_submission_package/ectd/0000/`
  (current build path uses `materialize_ectd.py` re-verify).
- `index-md5.txt` equals the MD5 of `index.xml` when the backbone is rebuilt from the current `m5/`.

## Scope / honest limits

- `xlink:href` are canonical intra-sequence paths; `checksum` is the real MD5 of the
  corresponding repo `08_submission_package/m5/` source file, except the generated STF leaf
  whose source is authored in the sequence. **Content is now materialized in-sequence** — see the
  Materialization section below.
- Official ICH/FDA **DTDs are now in place** (`ich-ectd-3-2.dtd`, `ich-stf-v2-2.dtd`,
  `us-regional-v3-3.dtd`) and all three backbone XML files are **DTD-valid** — see the
  "DTD validation" section below.
- Official ICH/FDA **stylesheets are now in place** (`ectd-2-0.xsl`,
  `ich-stf-stylesheet-2-3.xsl`, `us-regional.xsl`); every XML processing-instruction
  target resolves inside the sequence and all six support assets are SHA-256 pinned.
- `us-regional.xml` carries clearly-labelled **EXAMPLE** application metadata (`000000`) —
  supply the real FDA application/submission identifiers before submission.
- TFL appendix files under the controlled-study-report folder (figures/tables/listings)
  are included as study-report-body leaves; datasets, programs, defines, reviewer guides,
  blank CRF (source, not annotated), CSR PDFs, the informative simulation MAP/report,
  plain-text simulation program/config sources, and CSR appendices are all wired and tagged.
- First submission ⇒ every leaf `operation="new"`. Lifecycle (`replace`/`append`/
  `delete`) applies to later sequences per the ICH STF spec §IV–V.

## Materialization (refreshed 2026-08-14) — closes remediation 5a

`platform/materialize_ectd.py` reads the `index.xml` manifest and copies each leaf's
source file to its `xlink:href` location under `08_submission_package/ectd/0000/`, then re-verifies the copy's
MD5 against the recorded `checksum`. **Current sequence result (2026-08-14):** **98** sequence
m5 files present and indexed (**0** missing, **0** unindexed). Do not cite the older
**84/84** preview figure for this sequence.

Git hygiene: the materialized **payload is a reproducible copy of `08_submission_package/m5/` plus the
generated STF leaf** and is git-ignored
(datasets, `*.xpt`/`*.sas7bdat`/`*.json`, `*.pdf`/`*.png`/`*.xlsx`/`*.txt`); only the
backbone (`index.xml`, `index-md5.txt`), the Study Tagging File, the regional metadata,
and `util/` are tracked. Verified: **zero patient-data files are stageable**.

## DTD validation (2026-06-20) — all three backbone files DTD-VALID

With the official DTDs in `util/dtd/`, **all three eCTD XML files pass `xmllint --noout --valid`**:

| File | DTD | Result |
|---|---|---|
| `index.xml` | `ich-ectd-3-2.dtd` (ICH eCTD 3.2) | DTD-VALID |
| `m1/us/us-regional.xml` | `us-regional-v3-3.dtd` (FDA US Regional **v3.3**) | DTD-VALID |
| `.../tropic/stf-tropic.xml` | `ich-stf-v2-2.dtd` (ICH STF v2.2) | DTD-VALID |

Real bugs the DTDs surfaced and fixed in `build_ectd_backbone.py`:
- **xlink #FIXED typo** — ICH eCTD 3.2 and FDA us-regional v3.3 both hard-code the misspelled
  `http://www.w3c.org/1999/xlink`; the generator now emits the value the DTDs require.
- **`indication` #REQUIRED** added to `m5-3-5-reports-of-efficacy-and-safety-studies`.
- **us-regional migrated v2.01 → v3.3** (FDA retired v2.01; v3.3 required since 2022-03-01): new
  namespace `http://www.ich.org/fda`, `applicant-info` subtree, and the required
  `application-containing-files` / `application-type` / `submission-type` / `submission-sub-type`
  attributes. All identifiers are labelled **EXAMPLE** (`000000`) — replace with FDA-assigned
  values before any real submission.
- **STF retargeted** to ICH STF v2.2 (`dtd-version` 2.3 → 2.2); structure already conformed.

DTD and stylesheet support files are tracked and checksum-pinned; download archives and
the FDA specification PDFs are not part of the sequence.

## Reproduce

```bash
python3 platform/build_ectd_backbone.py   # backbone + STF + checksums
python3 platform/materialize_ectd.py      # copy 08_submission_package/m5/ content into the sequence + verify MD5
python3 platform/validate_ectd_sequence.py # full inventory/support/XML/checksum/run-record gate
```
